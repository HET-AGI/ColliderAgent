% Audit of every [AUTHORS: ...] marker and red TBD in paper.pdf, reply.pdf, sm.pdf
% (versions compiled 2026-09-16 04:10 / 05:53 EDT). Each item names the document, the page in
% that PDF, the table/section, what is asked, the status after the 2026-09-16 and 2026-09-24 measurements, and the
% text or numbers proposed. Items marked AUTHOR need a decision that no measurement can supply.
% Compile standalone: tectonic claims-audit-2026-09-24.tex
\documentclass[11pt]{article}
\usepackage[margin=2cm]{geometry}
\usepackage{booktabs,longtable,array,xcolor,hyperref,amsmath,slashed}
\newcommand{\statusdone}{\textcolor{green!50!black}{\textbf{DONE}}}
\newcommand{\statuspartial}{\textcolor{orange!80!black}{\textbf{PARTIAL}}}
\newcommand{\statusneedrun}{\textcolor{blue!70!black}{\textbf{RUNS}}}
\newcommand{\statusauthor}{\textcolor{red!70!black}{\textbf{AUTHOR}}}
\title{ColliderAgent revision: audit of open placeholders\\ \large measurements of 2026-09-16/17 and 2026-09-24, and proposed text}
\date{2026-09-24}
\begin{document}
\maketitle

\section*{Legend}
\statusdone\ = measured today, text below can be pasted. \statuspartial\ = partly measured; the rest is in progress.
\statusneedrun\ = needs the remaining benchmark runs (Opus 4.6 $\times$3 per benchmark; cross-model). \statusauthor\ = a decision or confirmation only the authors can make.

\section{Table S2 (sm.pdf p.\,4) --- software environment}
Status: \statusdone\ for every package (all read inside the images); only the Claude Code version of the \emph{original} runs is open (see note).
All versions were read inside the images on zhustation on 2026-09-16 with a probe blueprint
(\texttt{collider-env-probe}; job logs are cited per row). Two corrections to the current table: the analysis Python
stack lives in the \emph{agent runtime}, not in the collider image (the image has no \texttt{uproot}), and Delphes is 3.5.1.

\begin{center}
\begin{tabular}{lll l}
\toprule
Image / runtime & Package & Version & Evidence (2026-09-16) \\
\midrule
collider & MadGraph5\_aMC@NLO & 3.7.0 & \texttt{/opt/MG5\_aMC\_v3\_7\_0}, \texttt{MGMEVersion.txt} \\
collider & Pythia8 & 8.316 & \texttt{pythia8-config --version}; \texttt{PYTHIA\_VERSION 8.316} \\
collider & Delphes & 3.5.1 & \texttt{/opt/MG5\_aMC\_v3\_7\_0/Delphes/README} (installer tarball Delphes-3.5.1) \\
collider & MadAnalysis5 & 1.11.0 (2025-04-23) & \texttt{/opt/madanalysis5/version.txt}; HTML report banner \\
collider & LHAPDF & 6.5.5 & \texttt{HEPTools/bin/lhapdf-config --version} \\
collider & HepMC & 2.06.09 & header of \texttt{tag\_1\_pythia8\_events.hepmc.gz} (job 230c4933a9ab3b75) \\
collider & ROOT & 6.28/10 & \texttt{root-config --version} \\
collider & Python & 3.10.12; numpy 1.26.4, scipy 1.15.3, awkward 2.9.0, matplotlib 3.10.8, pyhf 0.7.6 & in-image \texttt{import} \\
mma-het & Wolfram Engine & 13.3.0 & \texttt{/usr/local/Wolfram/WolframEngine/13.3/.VersionID} \\
mma-het & FeynRules & 2.3.49 (29 Sep 2021) & \texttt{FeynRulesPackage.m}: \texttt{FR\$VersionNumber = "2.3.49"} \\
micromegas & micrOmegas / CalcHEP & 6.3.0 / 3.9.2 & \texttt{/opt/micromegas\_6.3.0}, \texttt{CalcHEP\_src/VERSION} (image \texttt{rise-agi/micromegas}) \\
agent runtime & Python analysis stack & Python 3.13.12; uproot 5.7.4, awkward 2.9.0, numpy 2.4.4, scipy 1.15.3, matplotlib 3.10.9, pyhf 0.7.6 & host \texttt{import}; installed 2026-04-19 .. 2026-06-08 \\
agent runtime & Claude Code & 2.1.273 (re-runs of 2026-09-16); original runs: see note & \texttt{claude --version} \\
agent runtime & LLM & Claude Opus 4.6 (\texttt{claude-opus-4-6}), still served on 2026-09-16 & one-turn probe call \\
\bottomrule
\end{tabular}
\end{center}

\noindent Proposed replacement rows (booktabs, same layout as the current table):
\begin{verbatim}
collider   & MadGraph5_aMC@NLO & 3.7.0 \\
           & Pythia8           & 8.316 \\
           & Delphes           & 3.5.1 \\
           & MadAnalysis5      & 1.11.0 \\
           & LHAPDF            & 6.5.5 \\
           & HepMC             & 2.06.09 \\
           & ROOT              & 6.28/10 \\
           & pyhf              & 0.7.6 \\
mma-het    & Wolfram Engine    & 13.3.0 \\
           & FeynRules         & 2.3.49 \\
agent runtime & Claude Code    & 2.1.x (AUTHORS: version of the original runs from the usage logs;
                                  the 2026-09-16 re-runs used 2.1.273) \\
           & LLM               & Claude Opus 4.6 \\
           & uproot, awkward, numpy, scipy & 5.7.4, 2.9.0, 2.4.4, 1.15.3 \\
\end{verbatim}
Note on the Claude Code version: no session log from the April--May runs exists on this machine (the earliest local
logs are from August 2026), so the original version has to come from the co-author who ran them (\statusauthor). The rows
above are otherwise complete.

\section{sm.pdf p.\,5, ``LLM inference accounts for a minor fraction [AUTHORS: fill in numbers ...]''}
Status: \statuspartial\ --- measured for the ALP EFT benchmark with three current models; the Opus 4.6 numbers for all
eight benchmarks are being produced now. The claim as written is \emph{not} supported for the light benchmarks:
attributing every gap before a model message to inference and every gap before a tool result to tool execution
(main session plus stage subagents), the model share of wall-clock was 38\% (Opus 5), 54\% (Opus 4.8) and 51\%
(Sonnet 5); time inside \texttt{magnus run}/\texttt{status} calls was 60\%, 45\% and 47\% (the two overlap when a
stage polls a running job). Proposed wording (with the 21 Opus 4.6 runs of Table S3, model share 0.23--0.80, median 0.55):
\begin{quote}
For the light benchmarks of Table S3 the wall-clock time splits roughly evenly between LLM inference and cluster
jobs (model share 0.2--0.8, median 0.55); for the Dark-SMEFT campaigns, where each campaign submits more than 100 jobs, cluster
time dominates.
\end{quote}

\section{Table S3 (sm.pdf p.\,6) --- resource usage; also paper.pdf p.\,5 and p.\,7, reply.pdf p.\,4 and p.\,6}
Status: \statuspartial\ --- Claude Opus 4.6 (effort \texttt{xhigh}, v2 skills, cold memory), three attempts per benchmark
for the seven figure benchmarks (attempt 1 launched 2026-09-16 22:00 UTC, attempts 2--3 on 2026-09-24). The two
Delphes-level benchmarks have one attempt each and stay TBD (footnotes a, b); their row shows that single attempt.
Values are means over the successful attempts ($n$ = successful/attempted; per-attempt values in
\texttt{docs/paper/results-2026-09-24/runs.csv}, figures in \texttt{.../figures/}).
Tokens = main session + all sub-agents (uncached + cache-write + cache-read); files = distinct paths written or edited;
Magnus jobs = \texttt{magnus run} submissions; USD from the CLI's \texttt{total\_cost\_usd}.

\begin{center}\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrrrrrrrrl}
\toprule
Benchmark & $n$ & Wall [h] & Sub-agents & Magnus jobs & Files & Tokens in [M] & Tokens out [k] & USD & Result \\
\midrule
Scalar LQ, $m_{ej}$ [Fig.\,2(a)] & 0/1 & 6.41 & 2 & 42 & 28 & 214.0 & 220 & -- & TBD$^{a}$ \\
ALP EFT, $\slashed{E}_T$ [Fig.\,2(b)] & 3/3 & 1.01 & 3.0 & 6.7 & 16.7 & 15.1 & 31 & 12.8 & success$^{*}$ \\
U(1)$'$ scan [Fig.\,2(c)] & 3/3 & 0.92 & 3.3 & 17.0 & 22.3 & 4.4 & 30 & 7.6 & success$^{*}$ \\
U1 LQ mono-$\tau$ [Fig.\,2(d)] & 0/1 & 4.00 & 2 & 29 & 19 & 181.4 & 219 & 106 & TBD$^{b}$ \\
Heavy N [Fig.\,S2(a,b)] & 3/3 & 0.60 & 3.0 & 9.0 & 14.3 & 3.9 & 17 & 4.8 & success$^{*}$ \\
General $Z'$ [Fig.\,S2(c)] & 3/3 & 0.73 & 3.0 & 7.3 & 14.3 & 8.3 & 25 & 7.2 & success$^{*}$ \\
KK graviton [Fig.\,S2(d)] & 3/3 & 0.95 & 2.7 & 9.3 & 14.3 & 3.8 & 28 & 6.4 & success$^{*}$ \\
U1 LQ at MuC, $\eta$ [Fig.\,S2(e)] & 2/3 & 0.68 & 3.0 & 14.0 & 6.0 & 4.4 & 18 & 7.8 & success$^{*}$, 1 failure$^{c}$ \\
U1 LQ at MuC, reach [Fig.\,S2(f)] & 3/3 & 1.22 & 3.0 & 19.0 & 21.7 & 13.3 & 39 & 12.7 & success$^{*}$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent$^{*}$ end-to-end reproduction with a quantitative deviation recorded as a footnote in at least one attempt
(texts in \texttt{docs/paper/results-2026-09-24/tables.md}). Ranges over the 21 attempts of the seven figure benchmarks:
0.37--1.41\,h, 3.6--22.2\,USD, 1.3--36.9\,M input tokens.\\
$^{a}$ \textbf{TBD (handed over).} Run stopped by the operator after 6.4\,h without a figure. Model, validation and the
100\,000-event LUXlep parton sample (lepton$\to$photon LHE workaround) were done; two Pythia8+Delphes launches of the
full sample finished on the cluster but failed at upload with ``No space left on device'' (the Delphes ROOT file alone is
27\,GB against the job's 10\,GB packing space); a third launch that deletes HepMC/ROOT after the run (MG5 \texttt{!} shell
lines after the final \texttt{done}, keeping the LHCO) was running when stopped. The v2 skills now document the limit and
the cleanup (commit \texttt{61800a9}); a rerun is expected to take 1.5--2\,h.\\
$^{b}$ \textbf{TBD (handed over).} Attempt 1 generated all 18 Delphes runs (4.0\,h, 106\,USD; four 9-run launches were lost
to the same disk limit before the agent split them), then the orchestrator ended its turn with ``waiting for event file
downloads'' and the headless session terminated without the analysis stage (failure mode (iv) in the paper's
classification). A continuation session in the same workspace produced the figure in 1.7\,h / 29.5\,USD with the reference's
shape but a 20--35\,\% weaker exclusion. The orchestrator skill now forbids ending a turn to wait (commit \texttt{e7c5b61}).\\
$^{c}$ Attempt 2 of U1 LQ at MuC, $\eta$: both leptoquark curves coincide with the SM background to 2--3\,\% per bin, i.e.\
the new-physics contribution did not enter event generation (failure mode (i), \emph{model}); attempts 1 and 3 reproduce the
reference (attempt 1 without triggering the orchestrator: the main session ran the pipeline itself, 0 sub-agent calls).

Model-latency share of wall-clock (transcript timestamps) over the 21 attempts of the figure benchmarks: 0.23--0.80,
median 0.55; the sentence ``LLM inference accounts for a minor fraction'' (sm.pdf p.\,5) should be replaced by, e.g.:
\begin{quote}
For the figure benchmarks of Table S3 the wall-clock time is shared about equally between LLM inference and cluster jobs
(model share 0.2--0.8, median 0.55 over 21 runs); cluster time dominates only for the Delphes-level benchmarks and the
Dark-SMEFT campaigns.
\end{quote}

\section{Tables S4 and S5 --- state on 2026-09-24}
Table S5 (robustness, Opus 4.6 $\times$3) is complete for the seven figure benchmarks: 20 of 21 attempts succeeded (the
one failure is footnote $c$ above). The two Delphes-level benchmarks have one attempt each (TBD$^{a}$, failure$^{b}$) and
still need their $\times$3 (1.5--6\,h and up to $>$100\,USD each at \texttt{xhigh}; use \texttt{--effort high}).

Table S4: column A = Opus 4.6 with Claude Code; column B = GPT-5.5 with Codex CLI 0.155.1 on branch \texttt{codex-com}
(skills-only mode, same prompts, one clean directory per attempt, same judge, metrics from the Codex event stream and the
main/sub-agent thread rollouts). GPT-5.3-Codex, the model contemporaneous with Opus 4.6, is not served to a ChatGPT-login
Codex account (an API key is required), so GPT-5.5 was used on 2026-09-24; on 2026-09-26 column B was re-run with
GPT-5.3-Codex through an API key and column C (Gemini via ADK) was measured --- see the next section.

\begin{center}\footnotesize\setlength{\tabcolsep}{3pt}
\begin{tabular}{lccc|ccc}
\toprule
 & \multicolumn{3}{c|}{Table S4 columns} & \multicolumn{3}{c}{single runs, 2026-09-16, default effort} \\
Benchmark & A: Opus 4.6 ($\times$3) & B: GPT-5.5 / Codex & C & Opus 4.8 & Opus 5 & Sonnet 5 \\
\midrule
Scalar LQ & 0/1$^{a}$ & 1/1$^{*e}$ (4.41\,h, 24 jobs, 75.0\,M tok) & -- & -- & -- & -- \\
ALP EFT & 3/3 & 1/1 (0.99\,h, 9 jobs, 13.4\,M tok) & -- & 1/1 & 1/1 & 1/1 \\
U(1)$'$ scan & 3/3 & 1/1 (1.03\,h, 21 jobs, 11.1\,M tok) & -- & -- & 1/1$^{*}$ & -- \\
U1 LQ mono-$\tau$ & 0/1$^{b}$ & 1/1$^{*d}$ (2.55\,h, 20 jobs, 49.4\,M tok) & -- & -- & -- & -- \\
Heavy N & 3/3 & -- & -- & -- & -- & -- \\
General $Z'$ & 3/3 & -- & -- & -- & -- & -- \\
KK graviton & 3/3 & -- & -- & -- & -- & -- \\
U1 LQ at MuC, $\eta$ & 2/3$^{c}$ & -- & -- & -- & -- & -- \\
U1 LQ at MuC, reach & 3/3 & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{center}

\noindent$^{d}$ Codex/GPT-5.5 completed mono-$\tau$ end to end (18 Delphes samples, 180\,000 events): the $R_{D^{(*)}}$ bands
match the reference, the exclusion boundary has the reference's shape but is about 30\,\% weaker ($\sqrt{|g_cg_b|}\approx1.07$
at 0.8\,TeV against $\approx0.8$). The Opus 4.6 continuation run of attempt 1 was 20--35\,\% weaker in the same way, so
two independent implementations agree with each other and not with the paper; the deviation is more likely a property of the
analysis procedure the prompt prescribes (the LHCO output carries no separate $\tau$ collection; both runs used type-4 jets
as hadronic-$\tau$ proxies) than an agent error.

\noindent$^{e}$ Codex/GPT-5.5 completed Scalar LQ end to end in 4.4\,h: 100\,000 LUXlep parton events, lepton$\to$photon LHE
substitution, Pythia8+Delphes (ATLAS card) to LHCO after two failed detector passes (the interactive re-shower could not
materialise \texttt{pythia8\_card.dat}; ``No space left on device''), the third as a non-interactive launch. The $m_{ej}$
signal peaks at 3\,TeV with $\approx1.5$ events/bin/100\,fb$^{-1}$ against $\approx3$ in the reference (factor 2 low) and ends
at 3.8\,TeV; the Delphes card used anti-$k_T$ $R=0.6$ instead of 0.4. Opus 4.6 attempt 1 was stopped at the same stage after
6.4\,h, so the benchmark is completable with the current skills and its Opus 4.6 $\times$3 is worth running.

Correction to the U(1)$'$ note of 2026-09-17: the Codex/GPT-5.5 run reproduces the paper's 3\,TeV contour tip
($\tilde g\approx-0.72$, $g_1'\approx0.65$), whereas all three Opus 4.6 attempts and the Opus 5 run give $g_1'\approx0.48$.
The 25\,\% shortfall is therefore an analysis choice made in the run, not a limitation of the prompt; the earlier
wording ``systematic to the prompt's analysis procedure'' is withdrawn (it stays a footnote, the runs stay successful under
the agreed criterion). The 2026-09-26 batch (next section) shows the same 0.48 tip in GPT-5.3-Codex and GPT-5.5/ADK runs,
so it is not Claude-specific either.

Success criterion used (agreed 2026-09-16): the run completes end to end and the figure agrees with the requested part of
the reference qualitatively; quantitative deviations go to a footnote ($^{*}$). Failure modes follow the paper's four
classes; every verdict was produced by the assistive judge (\texttt{scripts/bench/judge.sh}) and confirmed by eye.

Still open after 2026-09-26:
\begin{enumerate}
\item Scalar LQ and mono-$\tau$, Opus 4.6 $\times$3: \texttt{scripts/bench/run\_benchmark.sh 2005.06475 2 claude-opus-4-6
  --effort high} and \texttt{... 1811.07920 3 ...}; both root causes are fixed in the skills.
\item $\times$3 for the Opus 5 and GPT-5.3-Codex columns (one attempt each so far; about 1\,h per run).
\item A stricter model/harness separation: an Anthropic model inside the ADK loop (needs an Anthropic API key through LiteLLM).
\end{enumerate}

\section{Model C and the comprehensive Table S4 (2026-09-26)}
Model C is the repository's own \texttt{python-agent}: one Google ADK \texttt{LlmAgent} (ADK 2.10, LiteLLM) with 13 flat
tools (read/write/edit, FeynRules validation, UFO generation, MadGraph compile/launch, MadAnalysis, YAML workflow,
plus \texttt{run\_python}/\texttt{run\_shell} added on 2026-09-26 so that a figure can be drawn at all), no sub-agents,
no skills, no memory, no context compaction, \texttt{max\_llm\_calls}=150 (300 for the two Delphes benchmarks), a 6\,h
wall limit. Model: \texttt{gemini-3.1-pro-preview} (released 2026-02-19, the Gemini contemporaneous with Opus 4.6) through
an OpenAI-compatible relay. Two controls separate the harness from the model: the same Gemini model inside Gemini CLI 0.61
(a coding agent, same \texttt{.agents/skills} as the Codex harness, no custom sub-agents) and GPT-5.5 inside the same ADK
loop. Column B was re-run with GPT-5.3-Codex (API key). Same prompts, one clean directory per attempt, same judge; every run
was checked with \texttt{scripts/bench/job\_provenance.py} (footnote $f$).

\begin{center}\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabular}{lccc|c|cc}
\toprule
 & A: Opus 4.6 & A$'$: Opus 5 & B: GPT-5.3-Codex & \textbf{C: Gemini 3.1 Pro} & C$_1$: Gemini 3.1 Pro & C$_2$: GPT-5.5 \\
Benchmark & Claude Code ($\times$3) & Claude Code & Codex CLI & \textbf{ADK ($\times$3)} & Gemini CLI & ADK \\
\midrule
Heavy N & 3/3 & 1/1 & 1/1 & \textbf{1/3} & 1/1 & 1/1 \\
U(1)$'$ scan & 3/3 & 1/1 & 1/1 & \textbf{1/3} & 1/1 & 1/1 \\
ALP EFT & 3/3 & 1/1 & 1/1 & \textbf{2/3} & 1/1 & 1/1 \\
General $Z'$ & 3/3 & 1/1 & 1/1 & \textbf{1/3} & 1/1 & 1/1 \\
KK graviton & 3/3 & 1/1 & 1/1 & \textbf{2/3} & 1/1 & 1/1 \\
U1 LQ at MuC, $\eta$ & 2/3 & 1/1 & 0/1 & \textbf{0/3} & 1/1 & 1/1 \\
U1 LQ at MuC, reach & 3/3 & running & 0/1 & \textbf{1/3} & 1/1 & 1/1 \\
\midrule
figure benchmarks & 20/21 & 6/6 & 5/7 & \textbf{8/21} & 7/7 & 7/7 \\
\midrule
Scalar LQ (Delphes) & 0/1$^{a}$ & -- & 0/1$^{g}$ & running & -- & 0/1$^{h}$ \\
U1 LQ mono-$\tau$ (Delphes) & 0/1$^{b}$ & -- & 1/1$^{*}$ & running & -- & running \\
\bottomrule
\end{tabular}
\end{center}

Long-horizon indicators, figure benchmarks, means per run (the ADK event log records every model call and every tool's
success flag; the other harnesses do not expose comparable counts):

\begin{center}\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrrrrrrrrl}
\toprule
Column & runs & ok & wall [h] & LLM calls & tool calls & tool errors & refine cycles & max ctx [k tok] & exit \\
\midrule
Gemini 3.1 Pro / ADK & 21 & 8 & 1.41 & 117 & 117 & 39 & 39 & 120 & 10 capped \\
GPT-5.5 / ADK & 7 & 7 & 0.71 & 35 & 39 & 4 & 4 & 71 & completed \\
Gemini 3.1 Pro / Gemini CLI & 7 & 7 & 0.63 & -- & 72 & -- & -- & -- & completed \\
GPT-5.3-Codex / Codex & 7 & 5 & 0.81 & -- & -- & -- & -- & -- & -- \\
Opus 5 / Claude Code & 4 & 4 & 1.22 & -- & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{center}

Reading: (i) the same Gemini model goes from 8/21 in the single ADK loop to 7/7 in a coding-agent harness, with half the
wall-clock and no budget exhaustion --- the harness, not the model, is the main factor; (ii) GPT-5.5 survives the same
loop (7/7, 35 calls, 4 errors per run), i.e.\ the strongest current model masks the structural penalty on the figure
benchmarks; (iii) the Delphes-level benchmarks separate them again: the coding agents solve the lepton$\to$photon LHE
rewrite and the 10\,GB job limit by writing scripts and re-launching (Codex GPT-5.5 4.4\,h, GPT-5.3-Codex 2.6\,h for
mono-$\tau$ with the paper's exclusion), whereas GPT-5.5 in the ADK loop stopped at the parton level and smeared
$m_{ej}$ by hand (footnote $h$); (iv) ADK failures are loops --- 10 of 13 are the 150-call budget spent in event
generation or model building (108/78/121 failed tool calls and 379k/182k/230k-token contexts in the three MuC $\eta$
attempts) --- while the Codex/Claude failures are single physics errors (the MuC $\eta$ coupling not taking effect, an
empty contour plot).

\noindent$^{f}$ The Magnus job list is shared by all runs. The first GPT-5.3-Codex Heavy N run ``reproduced'' the figure in
7 minutes from the logs of the 2026-09-16 Opus 4.6 jobs; it was quarantined and re-run, every harness prompt now
carries \texttt{scripts/bench/benchmark\_rules.md} (submit your own jobs), and \texttt{job\_provenance.py} verified all other
52 runs of this batch. Earlier batches were not scanned (sandboxes deleted) but built their own models (5--42 jobs each).\\
$^{g}$ Model and UFO done; the agent could not install the LUXlep PDF set (\texttt{--pdf} installer lacks
\texttt{lhapdf-config} on zhustation) and stopped after 0.8\,h instead of using the MG5-internal \texttt{pdlabel/lhaid}
route that the Opus 4.6 and GPT-5.5 runs used.\\
$^{h}$ 100k LUXlep parton events, no Pythia8/Delphes/selection: the agent reported that the tool interface ``did not support
the requested LHE lepton-to-photon replacement before showering'' and applied a detector-resolution approximation to the
parton-level $m_{ej}$ (peak $\approx$180 events/bin against $\approx$4 in the reference); counted as a generation failure.\\
U(1)$'$ 3\,TeV tip, updated: $g_1'\approx0.48$ in Opus 4.6 $\times$3, Opus 5, GPT-5.3-Codex and GPT-5.5/ADK; 0.55 in Gemini
CLI; the paper's 0.65 only in Codex GPT-5.5 --- a common analysis choice, not a property of one model or of the prompt
(this supersedes the 2026-09-24 wording). Likewise for mono-$\tau$: the Claude continuation and Codex GPT-5.5 are 20--35\,\%
weak, GPT-5.3-Codex reproduces the paper (0.88 vs 0.8 at 0.8\,TeV).

Data: \texttt{docs/paper/results-2026-09-26/} (\texttt{README.md} in Chinese, \texttt{tables.md}, \texttt{runs.csv},
\texttt{longhorizon\_*.md}, \texttt{figures/}); harnesses \texttt{scripts/bench/run\_benchmark\_adk.sh} and
\texttt{run\_benchmark\_gemini.sh}.

\section{Other markers}
\begin{longtable}{p{3.2cm}p{5.6cm}p{1.6cm}p{6.2cm}}
\toprule
Where & Marker & Status & Proposed action \\
\midrule
\endhead
paper.pdf p.\,5 & ``skills-only mode executes the hello-world workflow of Sec.\,S-III [confirm that this has been tested]'' (Codex/Cursor) & \statusauthor & The Codex-compatible layout exists (branch \texttt{codex-com}: \texttt{.agents/skills}, \texttt{.codex/agents}, \texttt{AGENTS.md}). Run the hello-world prompt with Codex on that branch once and keep the sentence; otherwise weaken to ``is designed to''. \\
paper.pdf p.\,7, Table I caption & ``tokens consumed and wall-clock time [fill in]'' & \statusneedrun & Same numbers as Table S3 (Opus 4.6 column); cite Table S3 instead of repeating them. \\
paper.pdf p.\,9--10 & ``[confirm the extent of human intervention between campaigns]'' & \statusauthor & Dark-SMEFT campaigns were not re-run; statement must come from the campaign logs. \\
paper.pdf p.\,10; reply.pdf p.\,6 & ``several weeks of work for an experienced phenomenologist [adjust estimate]'' & \statusauthor & Judgement call; unchanged by measurements. \\
reply.pdf p.\,1 & checklist (1)--(4): fill S3/S4/S5, confirm human intervention, set \verb|\markupfalse|, remove red markers & \statuspartial & (1) in progress, (2) author, (3)--(4) mechanical once (1)--(2) are done. \\
reply.pdf p.\,4 & ``the LLM token consumption [fill in from the usage logs]'' & \statusneedrun & From Table S3; note the main+sub-agent definition. \\
reply.pdf p.\,4 & ``The wall-clock time of a benchmark (1--6 hours) ...'' & \statusdone & Consistent with today's runs (0.9--1.4 h for the lightest benchmark); keep. \\
sm.pdf p.\,4 & ``archived release ... DOI and per-run provenance packaged as Workflow Run RO-Crates [confirm or remove]'' & \statusauthor & No RO-Crate export exists in the repository today; either remove or keep as a plan. \\
sm.pdf p.\,5 & Table S4 placeholder text ``e.g. Claude Opus 4.6 vs. Claude Sonnet, GPT-5.x via Codex, Gemini via ADK'' & \statuspartial & Replace with the three columns chosen above; drop Gemini/ADK unless the python-agent path is run. \\
sm.pdf p.\,6 & Table S3 Dark-SMEFT rows (sub-agent calls, tokens) & \statusauthor & Not reproducible here; from the campaign logs. \\
\bottomrule
\end{longtable}

\section{Provenance of the numbers}
Branch \texttt{feat/skill-evolution-v2} of the ColliderAgent repository. All benchmark data are in
\texttt{docs/paper/results-2026-09-24/}: \texttt{tables.md} (Tables S3--S5 and every footnote text, generated by
\texttt{scripts/bench/aggregate.py}), \texttt{runs.csv} (one row per attempt), \texttt{documented\_runs.json} (the
2026-09-16 attempts, whose sandboxes were deleted; numbers copied from \texttt{docs/paper-tbd-audit-2026-09-16.md}) and
\texttt{figures/} (the produced figure of every attempt). The sandboxes under \texttt{bench\_runs/} were deleted after
aggregation. Image versions come from the probe blueprint \texttt{collider-env-probe} on zhustation. Skills used are the v2
skills (context-lean rewrite of 2026-09-16), so the Opus 4.6 numbers measure the current system.
\end{document}
