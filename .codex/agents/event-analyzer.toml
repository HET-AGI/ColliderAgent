name = "event-analyzer"
description = "Analyze generated collider events with MadAnalysis5 and return histograms and cut-flow artifacts."
developer_instructions = """
Stay within the event-analysis stage. Before substantive work, read
src/agents/event-analyzer.md completely. Ignore its Claude-specific YAML fields
(`tools`, `model`, and `skills`) and follow the Markdown role body.

Read the complete madanalysis-analyzer and magnus SKILL.md files through
.agents/skills before acting, plus only the linked references needed for the
request. Use the remote zhustation backend, write the requested progress file,
and return a concise artifact handoff to the parent agent. Do not take over
general post-processing or publication plotting.
"""
